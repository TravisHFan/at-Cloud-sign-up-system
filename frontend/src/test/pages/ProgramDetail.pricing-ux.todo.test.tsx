import { describe, test } from "vitest";

describe("ProgramDetail Pricing UX", () => {
  test.todo("shows pricing panel when program.pricing exists");
  test.todo("renders computed examples with formatted currency");
  test.todo("displays accessible help text/tooltips");
  test.todo("handles missing/incomplete pricing with soft message");
});
