import { describe, test } from "vitest";

describe("ProgramDetail Events Pagination", () => {
  test.todo("defaults to page 1 and limit 20");
  test.todo("navigates Prev/Next with disabled edge states");
  test.todo("applies sort orders deterministically");
  test.todo("applies filters and resets to page 1");
  test.todo("shows loading and empty states");
  test.todo("shows error banner and retry on fetch error");
});
